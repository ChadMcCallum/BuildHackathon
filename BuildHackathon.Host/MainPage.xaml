<Page
    x:Class="BuildHackathon.Host.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BuildHackathon.Host"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:common="using:BuildHackathon.Host.Common"
    mc:Ignorable="d">

	<Page.Resources>
		<common:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
		<common:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter" />
	</Page.Resources>

	<Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}">
		<TextBlock x:Name="pageMain" Margin="30,30,0,0" HorizontalAlignment="Left" TextWrapping="Wrap" Text="{StaticResource AppName}" VerticalAlignment="Top" Style="{StaticResource PageHeaderTextStyle}" />
		<Button x:Name="btnStartGame" Content="Start Game" FontSize="40" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,200" Height="335" Width="534" Tapped="btnStartGame_Tapped"/>
		<Button x:Name="btnEndGame" Content="End Game" FontSize="40" HorizontalAlignment="Center" VerticalAlignment="Bottom" Height="200" Width="300" Margin="0,0,0,100" Tapped="btnEndGame_Tapped" Visibility="{Binding Path=GameIsNull, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />

	</Grid>
</Page>
