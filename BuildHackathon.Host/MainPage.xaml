<Page
    x:Class="BuildHackathon.Host.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BuildHackathon.Host"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:common="using:BuildHackathon.Host.Common"
    mc:Ignorable="d">

	<Page.Resources>
		<common:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter" />
	</Page.Resources>

	<Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}">
		<TextBlock x:Name="pageMain" Margin="30,30,0,0" HorizontalAlignment="Left" TextWrapping="Wrap" Text="{StaticResource AppName}" VerticalAlignment="Top" Style="{StaticResource PageHeaderTextStyle}" />
		<Button x:Name="btnStartGame" Content="Start Game" FontSize="40" HorizontalAlignment="Left" Margin="29,163,0,0" VerticalAlignment="Top" Height="200" Width="300" Tapped="btnStartGame_Tapped"/>
		<Button x:Name="btnGoBackToCurrentGame" Content="Back To Game" FontSize="40" HorizontalAlignment="Left" VerticalAlignment="Top" Height="200" Width="300" Margin="404,163,0,0" Tapped="btnGoBackToCurrentGame_Tapped" Visibility="{Binding Path=GameIsNull, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
		
	</Grid>
</Page>
